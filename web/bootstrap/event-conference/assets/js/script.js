
const $=(s,el=document)=>el.querySelector(s), $$=(s,el=document)=>[...el.querySelectorAll(s)];
const safe=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const read=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}};
const save=(key,value)=>{try{localStorage.setItem(key,JSON.stringify(value));return true}catch{toast('Saved for this session. Browser storage is unavailable.');return false}};
let toastTimer;function toast(message){$('#toast').textContent=message;clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').textContent='',4500)}
$$('[data-open]').forEach(el=>el.addEventListener('click',()=>$('#'+el.dataset.open).showModal()));
$$('[data-close]').forEach(el=>el.addEventListener('click',()=>el.closest('dialog').close()));
$$('dialog').forEach(el=>el.addEventListener('click',e=>{if(e.target===el){const r=el.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)el.close()}}));
$$('.menu-toggle').forEach(el=>el.addEventListener('click',()=>{const target=document.getElementById(el.getAttribute('aria-controls'));const open=target.classList.toggle('open');el.setAttribute('aria-expanded',String(open))}));
$$('.nav-links a,.side-links a').forEach(a=>a.addEventListener('click',()=>{const parent=a.closest('.nav-links,.sidebar');if(parent){parent.classList.remove('open');const toggle=$('[aria-controls="'+parent.id+'"]');if(toggle)toggle.setAttribute('aria-expanded','false')}}));
function downloadFile(name,contents,type='text/plain'){const url=URL.createObjectURL(new Blob([contents],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
function csvCell(v){let s=String(v);if(/^[=+@\-\t\r]/.test(s))s="'"+s;return '"'+s.replaceAll('"','""')+'"'}

const speakers=[['Anika Poluru','Design Director, Layers','Anika explores how design systems can create room for individual expression. Her work bridges brand strategy and digital product design.'],['Dev Subra','Founder, Orbit','Dev builds tools for independent creators. At Unfold, he shares what happens when small teams choose a big, focused idea.'],['Mira Subbu','Creative Lead, Forma','Mira brings a human lens to emerging technology. Her session explores empathy, experimentation, and more thoughtful digital experiences.']];
$$('[data-speaker]').forEach(b=>b.addEventListener('click',()=>{const [name,role,bio]=speakers[b.dataset.speaker];$('#speaker-detail h2').textContent=name;$('#speaker-content').innerHTML='<span class="eyebrow">'+role+'</span><p>'+bio+'</p><p class="fine">Fictional speaker profile for this concept event.</p>';$('#speaker-detail').showModal()}));
const sessions=[{id:1,day:1,time:'09:00 AM',type:'Opening keynote',title:'A future worth making',speaker:'Anika Poluru',place:'Main stage'},{id:2,day:1,time:'10:30 AM',type:'Conversation',title:'Small teams. Unreasonably big ideas.',speaker:'Dev Subra',place:'Main stage'},{id:3,day:1,time:'01:00 PM',type:'Workshop',title:'Designing for the beautifully human',speaker:'Mira Subbu',place:'Studio A'},{id:4,day:1,time:'03:00 PM',type:'Community',title:'The conversations between the talks',speaker:'Hosted by Tara Poluru',place:'The courtyard'},{id:5,day:2,time:'09:30 AM',type:'Keynote',title:'The internet we want to live in',speaker:'Mira Subbu',place:'Main stage'},{id:6,day:2,time:'11:00 AM',type:'Workshop',title:'From blank page to bold idea',speaker:'Anika Poluru',place:'Studio A'},{id:7,day:2,time:'02:00 PM',type:'Panel',title:'What comes after the next big thing?',speaker:'Dev Subra & Mira Subbu',place:'Main stage'},{id:8,day:2,time:'04:00 PM',type:'Closing social',title:'This is only the beginning',speaker:'Hosted by Tara Poluru',place:'The courtyard'}];
let savedSessions=read('unfold-schedule',[]),day='1';
function renderSchedule(){const rows=sessions.filter(s=>day==='saved'?savedSessions.includes(s.id):String(s.day)===day);$('#saved-count').textContent=savedSessions.length;$('#schedule-rows').innerHTML=rows.length?rows.map(s=>'<article class="schedule-row"><time>'+(day==='saved'?'Sep '+(22+s.day)+'<br>':'')+s.time+'</time><div><span class="tag">'+s.type+'</span><h3>'+s.title+'</h3><p>'+s.speaker+'</p></div><span class="fine muted">'+s.place+'</span><button class="icon-btn" data-session="'+s.id+'" aria-label="'+(savedSessions.includes(s.id)?'Remove':'Save')+' '+s.title+'" aria-pressed="'+savedSessions.includes(s.id)+'">'+(savedSessions.includes(s.id)?'✓':'+')+'</button></article>').join(''):'<p class="empty">Your schedule is a blank canvas. Add sessions with the + button.</p>'}
$$('[data-day]').forEach(b=>b.addEventListener('click',()=>{day=b.dataset.day;$$('[data-day]').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',x===b)});renderSchedule()}));
$('#schedule-rows').addEventListener('click',e=>{const b=e.target.closest('[data-session]');if(!b)return;const id=Number(b.dataset.session);savedSessions=savedSessions.includes(id)?savedSessions.filter(s=>s!==id):[...savedSessions,id];save('unfold-schedule',savedSessions);renderSchedule()});renderSchedule();
let selectedPass='All-access pass';const rates={'Day pass':149,'All-access pass':249,'Team pass':199};function total(){const q=Number($('#pass-form [name=quantity]').value);$('#reservation-total').textContent=selectedPass+' · '+q+' × $'+rates[selectedPass]+' = $'+(q*rates[selectedPass]).toLocaleString()+' (demo total)'}
$$('[data-pass]').forEach(b=>b.addEventListener('click',()=>{selectedPass=b.dataset.pass;$('#pass-dialog h2').textContent=selectedPass;const q=$('#pass-form [name=quantity]');q.min=selectedPass==='Team pass'?3:1;q.value=q.min;total();$('#pass-dialog').showModal()}));$('#pass-form [name=quantity]').addEventListener('input',total);
$('#pass-form').addEventListener('submit',e=>{e.preventDefault();const data={...Object.fromEntries(new FormData(e.target)),pass:selectedPass};save('unfold-reservation',data);$('#pass-dialog').close();e.target.reset();toast('Demo reservation saved. No charge or real registration.')});
