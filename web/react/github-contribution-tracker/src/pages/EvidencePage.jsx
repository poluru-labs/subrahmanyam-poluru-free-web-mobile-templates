import { useApp } from '../context';
import { Badge, Button, Card, Icon, PageHeading } from '../components/ui';
export default function EvidencePage() { const { evidence } = useApp(); const exportReport = async (format) => { const response = await fetch(`/api/evidence/export?format=${format}`); const payload = await response.json(); const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' }); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = `contribly-evidence.${format === 'json' ? 'json' : 'txt'}`; link.click(); URL.revokeObjectURL(link.href); }; return <section className="space-y-6"><PageHeading eyebrow="Proof of impact" title="Evidence vault" description="Curate the context behind your work so your contribution story stays portable." action={<div className="flex gap-2"><Button variant="secondary" onClick={() => exportReport('json')}><Icon name="download" />Export</Button><Button><Icon name="plus-lg" />Add evidence</Button></div>} /><div className="grid gap-4 md:grid-cols-2">{evidence.map((item) => <Card key={item.id}><div className="flex items-start justify-between gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-soft text-brand"><Icon name="file-earmark-check" /></span><Badge>{item.type}</Badge></div><h2 className="mt-4 font-semibold">{item.title}</h2><p className="mt-1 text-sm text-brand">{item.source}</p><p className="mt-3 text-sm leading-6 text-muted">{item.notes}</p><div className="mt-4 flex flex-wrap gap-2">{item.tags.map((tag) => <span key={tag} className="rounded-md bg-canvas px-2 py-1 text-xs text-muted">#{tag}</span>)}</div></Card>)}</div></section>; }
