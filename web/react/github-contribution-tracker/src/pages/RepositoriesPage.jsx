import { useApp } from '../context';
import { Badge, Button, Card, Icon, PageHeading } from '../components/ui';
export default function RepositoriesPage() { const { repos } = useApp(); return <section className="space-y-6"><PageHeading eyebrow="Your watchlist" title="Repositories" description="Health signals and contribution context for the projects you follow." action={<Button><Icon name="plus-lg" />Add repository</Button>} /><div className="grid gap-4 lg:grid-cols-2">{repos.map((repo) => <Card key={repo.name}><div className="flex items-start gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-ink text-white"><Icon name="github" /></span><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><h2 className="font-semibold">{repo.owner}/{repo.name}</h2><Badge tone={repo.health > 90 ? 'success' : 'warning'}>{repo.health}% health</Badge></div><p className="mt-2 text-sm text-muted">{repo.description}</p><div className="mt-5 flex flex-wrap gap-5 text-xs text-muted"><span><strong className="text-ink">{repo.stars}</strong> stars</span><span><strong className="text-ink">{repo.openIssues}</strong> open issues</span><span><strong className="text-ink">{repo.language}</strong></span></div></div></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-canvas"><div className="h-full rounded-full bg-brand" style={{ width: `${repo.health}%` }} /></div></Card>)}</div></section>; }
