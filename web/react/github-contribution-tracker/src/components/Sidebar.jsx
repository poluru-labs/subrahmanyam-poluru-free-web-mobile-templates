import { NavLink } from 'react-router-dom';
import { navItems } from '../data';
import { useApp } from '../context';
import { Icon } from './ui';
export function Sidebar() { const { sidebarOpen, setSidebarOpen } = useApp(); return <>{sidebarOpen && <button type="button" className="fixed inset-0 z-30 bg-ink/40 lg:hidden" aria-label="Close navigation" onClick={() => setSidebarOpen(false)} />}<aside id="app-sidebar" className={`fixed inset-y-0 left-0 z-40 flex w-[248px] flex-col bg-ink text-white transition-transform duration-200 lg:translate-x-0 ${sidebarOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center gap-3 px-5 pb-2 pt-6"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand font-bold">GH</span><div><p className="ojd-font-display text-[17px] font-semibold">Contribly</p><p className="text-xs text-white/55">Open source workspace</p></div></div><div className="mx-4 mt-5 flex items-center gap-3 rounded-2xl bg-white/10 px-3 py-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-brand text-sm font-bold">SP</span><div><p className="text-sm font-semibold">Subbu Poluru</p><p className="text-xs text-white/55">Poluru · Contributor</p></div></div><nav className="mt-5 flex-1 px-3" aria-label="Primary"><ul className="space-y-1">{navItems.map((item) => <li key={item.to}><NavLink to={item.to} end={item.end} onClick={() => setSidebarOpen(false)} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition ${isActive ? 'bg-white text-ink' : 'text-white/72 hover:bg-white/10 hover:text-white'}`}><Icon name={item.icon} className="text-base" /><span>{item.label}</span></NavLink></li>)}</ul></nav><p className="px-5 pb-5 text-[11px] text-white/40">Sync status · connected</p></aside></>; }
