import { Link } from 'react-router-dom';
export function Icon({ name, className = '' }) { return <i className={`bi bi-${name} ${className}`} aria-hidden="true" />; }
export function Button({ children, variant = 'primary', className = '', ...props }) { const styles = { primary: 'bg-brand text-white hover:bg-brand-dark', secondary: 'border border-line bg-white text-ink hover:bg-canvas', ghost: 'text-brand hover:bg-brand-soft' }; return <button type="button" className={`inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold transition ${styles[variant]} ${className}`} {...props}>{children}</button>; }
export function Badge({ children, tone = 'brand' }) { const styles = { brand: 'bg-brand-soft text-brand-dark', success: 'bg-emerald-50 text-success', warning: 'bg-amber-50 text-warning', muted: 'bg-canvas text-muted' }; return <span className={`inline-flex items-center rounded-full px-2.5 py-1 text-[11px] font-semibold ${styles[tone]}`}>{children}</span>; }
export function Card({ title, kicker, action, children, className = '', bodyClassName = 'p-5' }) { return <section className={`rounded-2xl border border-line bg-white shadow-[0_1px_3px_rgba(26,42,51,.06)] ${className}`}>{(title || action) && <header className="flex flex-wrap items-center justify-between gap-3 border-b border-line px-5 py-4"><div>{kicker && <p className="mb-1 text-[11px] font-semibold uppercase tracking-[.16em] text-brand">{kicker}</p>}{title && <h2 className="font-semibold text-ink">{title}</h2>}</div>{action}</header>}<div className={bodyClassName}>{children}</div></section>; }
export function PageHeading({ eyebrow, title, description, action }) { return <header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-brand">{eyebrow}</p><h1 className="mt-1 ojd-font-display text-4xl font-semibold tracking-tight">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm text-muted">{description}</p>}</div>{action}</header>; }
export function TextLink({ to, children }) { return <Link to={to} className="text-sm font-semibold text-brand hover:underline">{children}</Link>; }
