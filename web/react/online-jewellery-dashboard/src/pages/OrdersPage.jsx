import { orders } from '../data';
import { Badge, Card, Icon } from '../components/ui';

export default function OrdersPage() {
  return <section className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand">Fulfilment</p><h1 className="mt-1 font-display text-4xl font-semibold tracking-tight">Orders</h1><p className="mt-2 text-sm text-muted">Keep every delivery moving with a quick view of today’s queue.</p></header><Card bodyClassName="p-0"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-line bg-canvas/60 text-xs uppercase tracking-wider text-muted"><tr><th className="px-5 py-4">Order</th><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Date</th><th className="px-5 py-4">Total</th><th className="px-5 py-4">Status</th></tr></thead><tbody className="divide-y divide-line">{orders.map((order) => <tr key={order.id} className="hover:bg-canvas/40"><td className="px-5 py-4 font-semibold">{order.id}</td><td className="px-5 py-4">{order.customer}</td><td className="px-5 py-4 text-muted">{order.date}</td><td className="px-5 py-4 font-semibold">${order.total}</td><td className="px-5 py-4"><Badge tone={order.status === 'Delivered' ? 'success' : order.status === 'Processing' ? 'brand' : 'warning'}><Icon name="circle-fill" className="mr-1 text-[7px]" />{order.status}</Badge></td></tr>)}</tbody></table></div></Card></section>;
}
