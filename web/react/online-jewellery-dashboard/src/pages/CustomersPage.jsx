import { customers } from '../data';
import { Avatar, Badge, Card, Icon } from '../components/ui';

export default function CustomersPage() {
  return <section className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand">Relationships</p><h1 className="mt-1 font-display text-4xl font-semibold tracking-tight">Customers</h1><p className="mt-2 text-sm text-muted">A considered view of the people behind every order.</p></header><div className="grid gap-4 md:grid-cols-2">{customers.map((customer) => <Card key={customer.email}><div className="flex items-start gap-4"><Avatar initials={customer.initials} className={customer.avatar} size="lg" /><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-semibold">{customer.name}</h2><Badge tone={customer.tier === 'VIP' ? 'brand' : 'muted'}>{customer.tier}</Badge></div><p className="mt-1 text-sm text-muted">{customer.email}</p><div className="mt-5 flex gap-6 text-sm"><span><strong className="block text-lg text-ink">{customer.orders}</strong><span className="text-muted">Orders</span></span><span><strong className="block text-lg text-ink">${customer.spent}</strong><span className="text-muted">Lifetime value</span></span></div></div><button className="rounded-lg p-2 text-muted hover:bg-canvas hover:text-brand" aria-label={`Open ${customer.name}`}><Icon name="arrow-up-right" /></button></div></Card>)}</div></section>;
}
